# Case-study-generator
A Claude Code slash command (/muhidcasestudy) that turns PRD, BRD, and designer story into a production-ready React case study page — 15 sections,      scroll-driven animations, per-element staggered reveals, PDF export, and image placeholders. Drop the .md in ~/.claude/commands/ and run.
